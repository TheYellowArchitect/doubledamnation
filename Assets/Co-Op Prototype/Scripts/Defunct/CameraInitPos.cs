using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraInitPos : MonoBehaviour 
{
	/*
	//Depending on starting variables, sets the camera starting pos.

	private GameManager gameManager;

	private GameObject temp;
	private Transform start;

	void Awake()
	{
		gameManager = GetComponent<GameManager> ();
		temp = GameObject.FindGameObjectWithTag("Player");
		start = temp.transform;
	}

	void Start () 
	{
		if (gameManager.controllerTesting)//==true
		{
			Vector3 position = new Vector3 (-13.25f, 1.85f, 3f) ;
			Quaternion rotation = Quaternion.Euler (90, 0, 0);
		}
		//else
			//start = somewhere near warrior guy
			
	}
	
	// Update is called once per frame
	void Update () 
	{
		
	}
	*/
}
